{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "directus start",
    "setup": "directus bootstrap && npm run schema:apply",
    "schema:apply": "directus schema apply ./schema.json -y",
    "schema:generate": "npx directus schema snapshot --format=\"json\" ./schema.json",
    "enter_shell": "docker-compose exec -it backend bash"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "directus": "^10.8.3",
    "pg": "^8.11.3"
  }
}
