{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "directus start",
    "setup": "directus bootstrap && directus schema apply ./schema.json -y",
    "enter_shell": "docker-compose exec -it backend bash",
    "schema": "npx directus schema snapshot --format=\"json\" ./schema.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "directus": "^10.6.3",
    "pg": "^8.11.3"
  }
}
